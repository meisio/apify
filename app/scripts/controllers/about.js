'use strict';

/**
 * @ngdoc function
 * @name apifyApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the apifyApp
 */
angular.module('apifyApp')
  .controller('AboutCtrl', function ($scope,$route) {
    $scope.$route = $route;
  });
